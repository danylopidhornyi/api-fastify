# Run prettier on staged files
yarn --cwd backend lint-staged

# Run tests
yarn --cwd backend test:run

# Run staged static package check (prevents commit if any pinned issues)
yarn --cwd backend check:static-packages-staged